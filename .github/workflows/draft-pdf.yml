name: Build JOSS PDF
on: [push]

jobs:
  paper:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Setup Pandoc
        run: |
          sudo apt-get update
          sudo apt-get install -y pandoc texlive-xetex texlive-latex-extra lmodern
      - name: Build PDF
        run: |
          pandoc paper.md --from=markdown --to=pdf --pdf-engine=xelatex --output=paper.pdf
      - uses: actions/upload-artifact@v4
        with:
          name: paper
          path: paper.pdf